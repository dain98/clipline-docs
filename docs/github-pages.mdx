---
id: github-pages
title: GitHub Pages
sidebar_position: 3
---

This site is configured for GitHub Pages project hosting at `/clipline-docs/`.

## Repository settings

In GitHub, open **Settings > Pages** for `dain98/clipline-docs` and set **Build and deployment > Source** to **GitHub Actions**.

## Automatic deployment

The deployment workflow runs on pushes to `main` and can also be started manually from the Actions tab.

```text
.github/workflows/deploy.yml
```

Pull requests run a build-only workflow:

```text
.github/workflows/test-deploy.yml
```

## Manual deployment

Docusaurus still includes the standard deploy command if you want to publish to the `gh-pages` branch manually:

```bash
GIT_USER=dain98 npm run deploy
```
